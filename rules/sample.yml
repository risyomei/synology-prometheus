groups:
- name: Alerting for Cloudcat
  rules:
  - alert: cloudcat is going to expire
    expr: script_exit_code{script="cloudcat"} != 0
    labels:
      severity: critical
      service: cloudcat
    annotations:
      summary: Cloudcat is expiring, go extend now
